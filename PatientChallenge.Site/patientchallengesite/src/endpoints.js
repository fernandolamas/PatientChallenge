const scheme = "https"
const port = "7105"
export const endpoint = {
    Patient: `${scheme}://localhost:${port}/Patient`,
    Account: `${scheme}://localhost:${port}/Account`,
    Swagger: `${scheme}://localhost:${port}/swagger/v1/swagger.json`
}

